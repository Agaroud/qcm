{% extends 'base.html.twig' %}

{% block body %}
    </br>
    <div class="text-center">
        <h4>Bienvenu dans l'interface Admin</h4>        
    </div>
    </br>
    </br>
    </br>
    <h4 class="text-center">Liste des salariés</h4>
    
    <table class="table">
        <tr>
            <th class="thead-light">Nom</th>
            <th class="thead-light">Prenom</th>
            <th class="thead-light">Matricule</th> 
            <th class="thead-light">Email</th>    
            <th class="thead-light">Derniere Note</th>                  
        </tr>
        {% for salarie in salaries %}
            <tr>
                <td>
                    {{ salarie.nom }}
                </td>
                <td>
                    {{ salarie.prenom }}
                </td>
                <td>
                    {{ salarie.matricule }}
                </td>
                <td>
                    {{ salarie.email }}
                </td>               
                
                <td>
                    <a href="{{ path('employe_scores', { 'idUser': salarie.id , 'firstNameUser' : salarie.prenom , 'nameUser' : salarie.nom}) }}">{{ salarie.derniereNote }}</a>
                </td>
                <td>
                    
                </td>
            </tr>
        {% endfor %}
    </table>    
{% endblock %}